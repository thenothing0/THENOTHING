{
 "amass_enum": {
  "params": [
   "domain",
   "passive"
  ],
  "required": [
   "domain"
  ]
 },
 "asset_lookup": {
  "params": [
   "asset"
  ],
  "required": [
   "asset"
  ]
 },
 "capability_list": {
  "params": [],
  "required": []
 },
 "capability_sources": {
  "params": [
   "capability",
   "online"
  ],
  "required": [
   "capability"
  ]
 },
 "check_tools": {
  "params": [],
  "required": []
 },
 "dalfox_scan": {
  "params": [
   "blind",
   "pipe_mode",
   "target",
   "timeout"
  ],
  "required": [
   "target"
  ]
 },
 "dirsearch_scan": {
  "params": [
   "extensions",
   "threads",
   "url"
  ],
  "required": [
   "url"
  ]
 },
 "dnsx_resolve": {
  "params": [
   "record_type",
   "target",
   "timeout",
   "wordlist"
  ],
  "required": [
   "target"
  ]
 },
 "ffuf_fuzz": {
  "params": [
   "fuzz_mode",
   "headers",
   "match_codes",
   "method",
   "url",
   "wordlist"
  ],
  "required": [
   "url"
  ]
 },
 "full_recon": {
  "params": [
   "domain"
  ],
  "required": [
   "domain"
  ]
 },
 "gau_urls": {
  "params": [
   "domain"
  ],
  "required": [
   "domain"
  ]
 },
 "generate_report": {
  "params": [
   "findings_json",
   "report_format",
   "target"
  ],
  "required": [
   "findings_json",
   "target"
  ]
 },
 "get_findings": {
  "params": [
   "limit",
   "severity"
  ],
  "required": []
 },
 "graph_neighbors": {
  "params": [
   "page"
  ],
  "required": [
   "page"
  ]
 },
 "graph_path": {
  "params": [
   "source_page",
   "target_page"
  ],
  "required": [
   "source_page",
   "target_page"
  ]
 },
 "gxss_check": {
  "params": [
   "timeout",
   "urls"
  ],
  "required": [
   "urls"
  ]
 },
 "hakrawler_crawl": {
  "params": [
   "depth",
   "plain",
   "scope",
   "target",
   "timeout"
  ],
  "required": [
   "target"
  ]
 },
 "httpx_probe": {
  "params": [
   "follow_redirects",
   "status_code",
   "targets",
   "tech_detect",
   "title"
  ],
  "required": [
   "targets"
  ]
 },
 "ingest_report": {
  "params": [
   "path",
   "source_url",
   "target",
   "text",
   "title"
  ],
  "required": []
 },
 "katana_crawl": {
  "params": [
   "depth",
   "js_crawl",
   "target"
  ],
  "required": [
   "target"
  ]
 },
 "kb_lint": {
  "params": [],
  "required": []
 },
 "kb_promote": {
  "params": [
   "evidence_count",
   "page",
   "scope_ok",
   "sources",
   "to_stage"
  ],
  "required": [
   "page",
   "to_stage"
  ]
 },
 "kb_rebuild_index": {
  "params": [],
  "required": []
 },
 "kb_recall": {
  "params": [
   "limit",
   "query",
   "target",
   "types"
  ],
  "required": [
   "query"
  ]
 },
 "list_reports": {
  "params": [
   "min_learning_score"
  ],
  "required": []
 },
 "nmap_scan": {
  "params": [
   "ports",
   "scan_type",
   "scripts",
   "target"
  ],
  "required": [
   "target"
  ]
 },
 "nuclei_scan": {
  "params": [
   "rate_limit",
   "severity",
   "tags",
   "target",
   "templates"
  ],
  "required": [
   "target"
  ]
 },
 "nuclei_scan_list": {
  "params": [
   "rate_limit",
   "severity",
   "tags",
   "targets"
  ],
  "required": [
   "targets"
  ]
 },
 "recon_fuse": {
  "params": [
   "capability",
   "domain",
   "online"
  ],
  "required": [
   "domain"
  ]
 },
 "report_lookup": {
  "params": [
   "slug"
  ],
  "required": [
   "slug"
  ]
 },
 "save_finding": {
  "params": [
   "description",
   "evidence",
   "finding_type",
   "severity",
   "target",
   "title"
  ],
  "required": [
   "description",
   "severity",
   "target",
   "title"
  ]
 },
 "sqlmap_scan": {
  "params": [
   "batch",
   "level",
   "risk",
   "tamper",
   "target",
   "technique",
   "timeout"
  ],
  "required": [
   "target"
  ]
 },
 "subfinder_scan": {
  "params": [
   "domain",
   "silent"
  ],
  "required": [
   "domain"
  ]
 },
 "wafw00f_detect": {
  "params": [
   "target"
  ],
  "required": [
   "target"
  ]
 },
 "whatweb_detect": {
  "params": [
   "aggression",
   "target"
  ],
  "required": [
   "target"
  ]
 }
}
